const menu=document.querySelector('.menu');
const nav=document.querySelector('.nav');
if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');}));}
const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
const form=document.getElementById('healthForm');
if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const sex=data.get('sex');const age=Number(data.get('age'));const height=Number(data.get('height'));const weight=Number(data.get('weight'));const factor=Number(data.get('activity'));if(!age||!height||!weight)return;const bmr=sex==='male'?10*weight+6.25*height-5*age+5:10*weight+6.25*height-5*age-161;const maintenance=bmr*factor;const bmi=weight/((height/100)**2);let bmiLabel='Healthy range';if(bmi<18.5)bmiLabel='Below healthy range';else if(bmi>=25&&bmi<30)bmiLabel='Above healthy range';else if(bmi>=30)bmiLabel='High range';const proteinLow=Math.round(weight*1.6);const proteinHigh=Math.round(weight*2.0);const water=(weight*0.035).toFixed(1);document.getElementById('bmrValue').textContent=Math.round(bmr)+' kcal';document.getElementById('maintValue').textContent=Math.round(maintenance)+' kcal';document.getElementById('bmiValue').textContent=bmi.toFixed(1);document.getElementById('bmiLabel').textContent=bmiLabel;document.getElementById('proteinValue').textContent=proteinLow+'–'+proteinHigh+' g';document.getElementById('waterValue').textContent=water+' L';document.getElementById('lossValue').textContent=Math.round(maintenance-450)+'–'+Math.round(maintenance-250)+' kcal';document.querySelector('.empty-state').hidden=true;document.querySelector('.result-grid').hidden=false;document.querySelector('.result-cta').hidden=false;});}
