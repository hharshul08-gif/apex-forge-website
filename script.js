const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');if(menuBtn&&nav){menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const form=document.getElementById('healthForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const sex=d.get('sex'),age=Number(d.get('age')),h=Number(d.get('height')),w=Number(d.get('weight')),activity=Number(d.get('activity'));if(!age||!h||!w)return;const bmr=Math.round(10*w+6.25*h-5*age+(sex==='male'?5:-161));const maint=Math.round(bmr*activity);const bmi=w/((h/100)**2);let label='Healthy range';if(bmi<18.5)label='Below healthy range';else if(bmi>=25&&bmi<30)label='Above healthy range';else if(bmi>=30)label='High range';const loss=Math.max(1200,Math.round(maint*.82));const gain=Math.round(maint*1.08);const proteinLow=Math.round(w*1.6),proteinHigh=Math.round(w*2.0);const water=(w*.035).toFixed(1);const minW=(18.5*((h/100)**2)).toFixed(1),maxW=(24.9*((h/100)**2)).toFixed(1);document.getElementById('bmrValue').textContent=bmr;document.getElementById('maintValue').textContent=maint;document.getElementById('bmiValue').textContent=bmi.toFixed(1);document.getElementById('bmiLabel').textContent=label;document.getElementById('lossValue').textContent=loss;document.getElementById('gainValue').textContent=gain;document.getElementById('proteinValue').textContent=`${proteinLow}–${proteinHigh} g`;document.getElementById('waterValue').textContent=`${water} L`;document.getElementById('weightRangeValue').textContent=`${minW}–${maxW} kg`;document.querySelector('.result-empty').hidden=true;document.querySelector('.result-grid').hidden=false;document.getElementById('resultCta').hidden=false})}
document.getElementById('year').textContent=new Date().getFullYear();const observer=new IntersectionObserver(entries=>entries.forEach(x=>{if(x.isIntersecting)x.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
